#include "shell.h"

/**
 * _strdup - like strdup
 * @str: input
 *
 * Return: pointer to duplicate
 */

char *_strdup(char *str)
{
	size_t len;
	char *dup;

	len = _strlen(str);
	dup = (char *)err_malloc(len + 1);

	_strcpy(dup, str);

	return (dup);
}
